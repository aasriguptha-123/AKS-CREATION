# Deploy to Azure Kubernetes Service
# Build and push image to Azure Container Registry; Deploy to Azure Kubernetes Service
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger: none

resources:
- repo: self

variables:

  # Container registry service connection established during pipeline creation
  dockerRegistryServiceConnection: '08891040-c649-4d02-abf3-0be08bda7b0d'
  imageRepository: 'phpapp'
  containerRegistry: 'aksdemoclusteracr.azurecr.io'
  dockerfilePath: 'AKS-HPA/Dockerfile'
  tag: '$(Build.BuildId)'
  imagePullSecret: 'aksdemoclusteracr8924e072-auth'

  # Agent VM image name
  vmImageName: 'ubuntu-latest'


stages:
- stage: Build
  displayName: Build stage
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: $(vmImageName)
    steps:
    - task: Docker@2
      displayName: Build and push an image to container registry
      inputs:
        command: buildAndPush
        repository: $(imageRepository)
        dockerfile: $(dockerfilePath)
        containerRegistry: $(dockerRegistryServiceConnection)
        tags: |
          $(Build.BuildId)
    - script: |
          echo "Inside AKS-HPA:"
          ls -R AKS-HPA
          echo "Inside kube-manifests:"
          ls -l AKS-HPA/kube-manifests
          displayName: DEBUG DEEP

    - upload: manifests
      artifact: manifests

